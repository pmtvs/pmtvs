[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "pmtvs-distance"
version = "0.3.3"
description = "Distance metrics for signal comparison (4 functions, 3 Rust-accelerated)"
readme = "README.md"
license = {text = "PolyForm-Strict-1.0.0"}
license-files = []
authors = [{name = "pmtvs contributors"}]
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy>=1.20",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-benchmark>=4.0",
]

[project.urls]
Homepage = "https://github.com/pmtvs/pmtvs"
Repository = "https://github.com/pmtvs/pmtvs"

[tool.maturin]
python-source = "python"
module-name = "pmtvs_distance._rust"
features = ["pyo3/extension-module"]

[tool.pytest.ini_options]
testpaths = ["tests"]
