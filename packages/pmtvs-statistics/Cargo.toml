[package]
name = "pmtvs_statistics"
version = "0.1.0"
edition = "2021"

[lib]
name = "_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
numpy = "0.21"
pmtvs_shared = { path = "../../shared" }

[profile.release]
opt-level = 3
lto = true
